spring:
  application:
    name: fakebank
  datasource:
    url: jdbc:mysql://192.168.0.115:13306/fakebank?serverTimezone=Asia/Seoul
    hikari:
      connection-test-query: select 1
      idle-timeout: 5000
      max-lifetime: 120000
      maximum-pool-size: 5
      minimum-idle: 5
  cloud:
    vault:
      uri: http://192.168.0.115:8200
      connection-timeout: 5000
      read-timeout: 15000
      # ssl:
      #   trust-store: classpath:/vault.jks
      #   trust-store-password: changeit
      generic:
        enabled: true
        application-name: fakebank
      #      kv:
      #        enabled: false
      #        backend: kv
      #        application-name: fakebank
      database:
        enabled: true
        role: fakebank-accounts-ro
        backend: database
        username-property: spring.datasource.username
        password-property: spring.datasource.password
      token: s.5yWHkcZ1LOAAGQQkYuC3LmQR
  config:
    import: vault://
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
server:
  port: 8089